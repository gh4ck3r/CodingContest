#include <iostream>
#include <vector>

using namespace std;

int main()
{
  size_t c, n;
  cin >> c;
  while (c--) {
    cin >> n;

    vector<int> lengths(n);
    for (auto &l : lengths) cin >> l;

  }
  return 0;
}
